// Ambient declarations for non-code assets handled by the Next.js compiler.
// CSS Modules (*.module.css) are typed by Next's bundled declarations; this covers the
// global stylesheet side-effect import so a standalone `tsc --noEmit` resolves it too.
declare module '*.css';
