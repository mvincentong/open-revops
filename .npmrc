# pnpm configuration. See https://pnpm.io/npmrc
auto-install-peers=true
strict-peer-dependencies=false
# Keep the lockfile authoritative; CI installs with --frozen-lockfile.
prefer-frozen-lockfile=true
