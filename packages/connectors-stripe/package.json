{
  "name": "@open-revops/connectors-stripe",
  "version": "0.0.0",
  "private": true,
  "description": "Stripe billing action adapters (write path) — apply_credit behind a narrow, typed, approval-gated interface.",
  "license": "Apache-2.0",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "files": ["dist"],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit -p tsconfig.json",
    "build": "tsc -p tsconfig.build.json"
  },
  "dependencies": {
    "stripe": "^22.2.1"
  },
  "devDependencies": {
    "@types/node": "^25.9.2"
  }
}
