#!/usr/bin/env node
// Placeholder dev runner for the scaffold phase. Replace with the real orchestration
// (operator UI + agent API) as apps/web and apps/api land.
console.log(`
OpenRevOps — dev (scaffold placeholder)

The product apps are not implemented yet. When apps/web and apps/api land, this script
will start the operator UI and the agent API together. For now:

  pnpm demo:seed     seed deterministic synthetic data
  pnpm demo:reset    reset demo state
  pnpm -r dev        run package dev scripts once they exist

See docs/architecture.md and ROADMAP.md.
`);
